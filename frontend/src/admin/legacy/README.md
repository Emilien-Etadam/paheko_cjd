# Legacy CSS (archives)

Les styles métier ont été migrés vers `../components/`.

Fichiers conservés à titre de référence uniquement (non importés par le build) :

- `01-layout.css` — ancien layout latéral
- `05-navigation.css` — ancien menu `#menu`

Le bundle admin charge uniquement `../components/` via `admin.css`.
